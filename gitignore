# !! SECRET — never commit these !!
config.js
.env

# Node dependencies — reinstalled with npm install
node_modules/
package-lock.json

# Electron build output — distribute separately, not via GitHub
dist/

# OS junk
.DS_Store
Thumbs.db
*.log
